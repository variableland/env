pre-commit:
  parallel: true
  jobs:
    - name: jscheck
      run: pnpm rr jscheck --fix-staged
      stage_fixed: true

    - name: tscheck
      run: pnpm rr tsc
      glob:
        - "{package,docsite}/**/*.{ts,tsx}"
        - "{package,docsite}/tsconfig.json"

pre-push:
  jobs:
    - name: test
      run: pnpm --filter=@vlandoss/env test
